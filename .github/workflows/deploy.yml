name: Deploy PrestaShop to Server

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Create parameters.yml
        run: |
          mkdir -p app/config
          cat <<EOF > app/config/parameters.yml
          parameters:
              database_host: 127.0.0.1
              database_port: 3306
              database_name: vipartes_ps
              database_user: vipartes_admin
              database_password: ${{ secrets.DB_PASS }}
              database_prefix: ps_
              database_engine: InnoDB
              mailer_transport: smtp
              mailer_host: 127.0.0.1
              secret: ${{ secrets.PS_SECRET }}
              ps_caching: CacheMemcache
              ps_cache_enable: false
              ps_creation_date: '2024-01-01'
              locale: es-ES
              use_html_purifier: true
              cookie_key: ${{ secrets.PS_COOKIE_KEY }}
              cookie_iv: ${{ secrets.PS_COOKIE_IV }}
              new_cookie_key: ${{ secrets.PS_NEW_COOKIE_KEY }}
          EOF

      - name: Deploy via rsync
        uses: Burnett01/rsync-deployments@7.0.1
        with:
          switches: -avzr --delete --exclude='.git*' --exclude='var/cache/*' --exclude='vendor/*'
          path: ./
          remote_path: /var/www/vhosts/vipartesanias.ubbie.studio/httpdocs/
          remote_host: 148.251.42.8
          remote_user: ubbie_vipartesanias
          remote_key: ${{ secrets.SERVER_SSH_KEY }}

      - name: Server Post-Deployment Tasks
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: 148.251.42.8
          username: ubbie_vipartesanias
          key: ${{ secrets.SERVER_SSH_KEY }}
          script: |
            export PHP_BIN="/opt/plesk/php/8.2/bin/php"
            cd /var/www/vhosts/vipartesanias.ubbie.studio/httpdocs/
            
            # 1. Update Database
            mysql -u vipartes_admin -p'${{ secrets.DB_PASS }}' vipartes_ps -e "UPDATE ps_shop_url SET domain='vipartesanias.ubbie.studio', domain_ssl='vipartesanias.ubbie.studio', physical_uri='/'; UPDATE ps_configuration SET value='vipartesanias.ubbie.studio' WHERE name='PS_SHOP_DOMAIN' OR name='PS_SHOP_DOMAIN_SSL';"
            
            # 2. Setup Composer
            rm -f composer.phar
            $PHP_BIN -r "copy('https://getcomposer.org/installer', 'composer-setup.php');"
            $PHP_BIN composer-setup.php

            # 3. Nuclear Stability Fix
            $PHP_BIN -r "
              \$json = json_decode(file_get_contents('composer.json'), true);
              \$json['minimum-stability'] = 'dev';
              \$json['prefer-stable'] = true;
              file_put_contents('composer.json', json_encode(\$json, JSON_PRETTY_PRINT));
            "

            # 4. THE SIMPLEST REQUIRE (No complex flags to break it)
            rm -rf vendor composer.lock
            $PHP_BIN composer.phar require prestashop/prestashop:8.2.* --no-interaction --ignore-platform-reqs --no-security-blocking

            # 5. Fix Autoload Bridge
            cat <<'EOF' > config/autoload.php
            <?php
            if (file_exists(__DIR__ . '/../vendor/autoload.php')) {
                require_once __DIR__ . '/../vendor/autoload.php';
            }
            $internalLoader = __DIR__ . '/../src/PrestaShop/Autoload/PrestashopAutoload.php';
            if (file_exists($internalLoader)) {
                require_once $internalLoader;
                if (!class_exists('PrestaShop\Autoload\PrestashopAutoload') && class_exists('PrestashopAutoload')) {
                    class_alias('PrestashopAutoload', 'PrestaShop\Autoload\PrestashopAutoload');
                }
            }
            if (class_exists('PrestaShop\Autoload\PrestashopAutoload')) {
                PrestaShop\Autoload\PrestashopAutoload::getInstance()->register();
            }
            EOF

            # 6. Cleanup
            rm -rf var/cache/*
            find . -name ".git" -prune -o -type d -exec chmod 755 {} +
            find . -name ".git" -prune -o -type f -exec chmod 644 {} +
            
            echo "Running Bootstrap Test..."
            $PHP_BIN -r "require 'config/config.inc.php'; echo 'Full Bootstrap Success';"